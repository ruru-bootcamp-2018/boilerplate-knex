<!DOCTYPE html>
<html>
  <head>
    <title>Pickapath</title>
  </head>
  <body>
    <ul>
    {{#each paths}}
      <li><a href="/path/{{id}}">{{story}} ({{ch1}}) ({{ch2}})</a></li>
    {{/each}}
    </ul>
  </body>
</html>
