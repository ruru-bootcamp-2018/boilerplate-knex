<!DOCTYPE html>
<html>
  <head>
    <title>EDA Phase 1 Express Boilerplate</title>
  </head>
  <body>
    This is a view of lots of profiles
    <ul>
    {{#each profiles}}
      <li>{{name}}, ({{email}}), {{URL}}, <img src="{{profile_pic}}"></img></li>
    {{/each}}
    </ul>
  </body>
</html>